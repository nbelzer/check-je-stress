<?php

$organisatie = isset($_GET['organisatie']);
$code = isset($_GET['code']);

if ($organisatie && $code) {
  // Toon de personeelstest
} else {
  // Toon pagina met informatie over de personeelstest
}
